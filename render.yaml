services:
  - type: web
    name: pulse-quiz
    runtime: docker
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: pulse-data
      mountPath: /app/data
      sizeGB: 1
